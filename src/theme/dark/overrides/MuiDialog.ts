import palette from "../palette";

export default {
    styleOverrides: {
        paper: {
            backgroundColor: palette.background.paper,
            backgroundImage: 'none'
        },

    }
}
